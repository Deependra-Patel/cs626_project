create table student(
	id integer primary key,
	name varchar(50),
	department varchar(20),
	hostel integer,
	credits integer,
	cpi numeric(4,2)
)
create table instructor(
	id integer primary key,
	name varchar(50),
	department varchar(20),
	salary integer
)
departments: computer, electrical, mechanical, metallurgy, chemical
